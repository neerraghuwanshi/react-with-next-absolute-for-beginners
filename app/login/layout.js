export default function LoginLayout(data) {
    const { children } = data;
    return (
        <section>
            <h1>
                Login Layout
            </h1>
            {children}
        </section>
    )
}
